parameters:
    level: 5
    paths:
        - src
    ignoreErrors:
        -
            # Only consumed outside src/ so PHPStan can't see any usage within the analysed paths.
            identifier: trait.unused
            path: src/Policy/PolicyTrait.php
